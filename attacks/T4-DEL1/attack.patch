--- a/src/smf/gy-path.c
+++ b/src/smf/gy-path.c
@@ -282,7 +282,7 @@
                                                       uint32_t cc_request_type, struct msg *req)
 {
     int ret;
-    bool include_rating_group = true;
+    bool include_rating_group = false;
     union avp_value val;
     struct avp *avp;
     struct avp *avpch1;
@@ -291,7 +291,7 @@
     ret = fd_msg_avp_new(ogs_diam_gy_multiple_services_cc, 0, &avp);
     ogs_assert(ret == 0);
 
-    /* selected-standard-scenario W6 fixture: use the supported differentiated RG 6 tariff. */
+    /* selected-standard-scenario W6 fixture: delete the supported differentiated RG 6. */
     if (include_rating_group) {
         ret = fd_msg_avp_new(ogs_diam_gy_rating_group, 0, &avpch1);
         ogs_assert(ret == 0);
@@ -304,6 +304,9 @@
     ogs_warn("selected-standard-scenario built MSCC request_type[%u] "
             "rating_group_present[%u] rating_group[%u]",
             cc_request_type, include_rating_group, 6);
+    if (!include_rating_group)
+        ogs_warn("selected-standard-scenario deleted Rating-Group from MSCC request_type[%u]",
+                cc_request_type);
 
     /* Requested-Service-Unit, RFC4006 8.18 */
     if (cc_request_type == OGS_DIAM_GY_CC_REQUEST_TYPE_INITIAL_REQUEST ||
